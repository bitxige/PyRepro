# Contributing

Create a feature branch from `main` and open a pull request for changes.
Keep `main` runnable, explain the motivation and testing in the pull request,
and link a related issue when one exists.

Before opening a pull request, run:

```bash
ruff check .
ruff format --check .
pytest
```
