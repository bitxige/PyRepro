"""Unrelated debug ballast for the P0 reducer fixture."""


def debug_label(identifier: str) -> str:
    """Return a debug label.

    Args:
        identifier: Identifier to label.

    Returns:
        Debug label.
    """
    return f"debug:{identifier}"
