"""Unrelated visualization ballast for the P0 reducer fixture."""


def render_lane(identifier: str) -> str:
    """Return a display label for a lane identifier.

    Args:
        identifier: Lane identifier to display.

    Returns:
        Rendered display label.
    """
    return f"Lane {identifier}"
