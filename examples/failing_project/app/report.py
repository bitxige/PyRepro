"""Unrelated reporting ballast for the P0 reducer fixture."""


def summarize(identifier: str) -> str:
    """Return a one-line summary.

    Args:
        identifier: Identifier to summarize.

    Returns:
        Summary text.
    """
    return f"summary:{identifier}"
