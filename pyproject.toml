[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reposentinel"
version = "0.1.0"
description = "Evidence-oriented static review infrastructure for Python repositories."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "RepoSentinel contributors" }]
dependencies = []

[project.optional-dependencies]
dev = [
    "pre-commit>=3.7",
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.scripts]
reposentinel = "reposentinel.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["reposentinel*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"

[tool.ruff]
line-length = 88
target-version = "py310"
src = ["reposentinel", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "D100", "D101", "D102", "D103", "D104", "D107"]
ignore = ["D203", "D213"]

[tool.ruff.lint.per-file-ignores]
"examples/sample_project/**" = ["D100", "D101", "D102", "D103", "D104", "D107"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
